apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: employee-hub-eks
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Yatingambhir85/Employee-CRUD.git
    targetRevision: main
    path: k8s-eks
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: employee-hub
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
